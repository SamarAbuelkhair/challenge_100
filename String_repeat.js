// function repeat the given string
function repeatstr(n, s) {
    return s.repeat(n);
    // var newstr = "";
    // for (i = 0; i < n; i++){
    //     newstr += s;
    // }
    // console.log(newstr);
}
console.log(repeatstr(6, "Hello"));